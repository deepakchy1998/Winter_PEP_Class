// 3. Write an arrow function called square that takes a number as an argument and returns its square.

const findSquareRoot = (x) => {
	return Math.sqrt(x);
};

let result = findSquareRoot(625);
console.log(result);
